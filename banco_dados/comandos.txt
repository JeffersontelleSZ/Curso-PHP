## O que e mySQL
O MySQL é um sistema de gerenciamento de banco de dados, que utiliza a linguagem SQL como interface

###########################################

# Criando e excluindo o banco de dados
>> Criar
  CREATE DATABASE nome_do_banco;
>> Excluir
  DROP DATABASE nome_do_banco;

# Tipos de dados
>> campos de textos
>> campos numericos
>> campos da data e hora

# Criar e excluir tabelas
>> Criar
  CREATE TABLE nome_tabela;
>> Excluir
  DROP TABLE nome_tabela;

# Renomeando o nome da tabela
>> RENAME TABLE nome_atual TO nome_mudanca; 

# Incluir, Editar e Remover Coluna
>> ALTER TABLE
 > ADD
    ALTER TABLE nome_tabela ADD COLUMN nome_coluna varchar(5) not null;
 > CHANGE
   ALTER TABLE nome_tabela CHANGE novo_nome; 
 > DROP
    DROP TABLE nome_tabela;

# INSERT - Enserindo conteudo nas colunas
  INSERT INTO nome_tabela(nomes_dados)VALUE(valor_dados);

# SELECT - Selecionando colunas na tabela  
  SELECT <COLUNA> FROM <TABELA>;

# WHERE - Filtrando registros
  SELECT <COLUNA> FROM <TABELA> WHERE <COLUMN> <= VALOR;





